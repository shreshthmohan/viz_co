When you pass improper arguments to a function, VSCode will show you typescript errors. As an example see error in `demo_ts/index.ts`.

You can also see the error by running `tsc` from `demo_ts` folder
