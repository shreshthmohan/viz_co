declare namespace _default {
    const input: string;
    namespace output {
        const file: string;
        const format: string;
        namespace globals {
            const lodash: string;
            const d3: string;
        }
    }
    const plugins: any[];
    const external: string[];
}
export default _default;
